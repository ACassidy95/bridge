#include <stdio.h>
#include <stdlib.h>
#include "utils.h"

const char* get_user_input(const char* template) {
        
}

void quit(const char* err) {
        fprintf(stderr, "%s\n", err);
        exit(1);
}

input_error:
